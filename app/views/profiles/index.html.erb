<div class="container">
	<div class="row">
		<h1>Alle Profile</h1>
		   <%= paginate @profiles %>
		      <% @profiles.each do |p| %>
		        <h3><%= link_to p.Titel, profile_path(p) %></h3>
		        <hr>
		      <% end %>
		   <%= paginate @profiles %>
    </div>
</div>
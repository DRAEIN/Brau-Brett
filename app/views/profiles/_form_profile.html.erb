<%= form_for(@profile) do |p| %>
	<%= p.label :Titel %><br />
	<%= p.text_field :Titel %>

	<%= p.label :Name %>
	<%= p.text_field :Name %>

	<%= p.label :Beschreibung %>
	<%= p.text_area :Beschreibung %> 

	<hr>

	<%= p.submit "Profil erstellen" %>
<% end %> 
 
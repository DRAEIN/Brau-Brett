<div class="container">
  <div class="row">
    <div class="col-md-6">
	  <h1><%= @job.Titel %><h1>
	  <h3>Beschreibung</h3>
	    <%= @job.Beschreibung %>
	  <h3>Ort</h3>
	    <%= @job.Ort %>
	  <% if current_user == @job.user %>
        <p><%= link_to 'Bearbeiten', edit_job_path %>
	       <%= link_to 'Löschen', @blog, method: :delete, data: { confirm: "Sind Sie sicher?" } %>
	    </p>
	  <% end %>
	</div>
	<div class="cold-md-6">
	  <% if @job.avatar? %>
        <%= image_tag @job.avatar.url(:medium) %>
	  <% else %>
	    Kein Bild vorhanden!
	  <% end %>
	</div>
  </div>
</div>